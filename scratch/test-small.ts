// Fichier court (< 100 lignes) pour tester investigate_file
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export function add(a: number, b: number): number {
  return a + b;
}

const PI = 3.14159;

export interface User {
  id: string;
  name: string;
  email: string;
}
